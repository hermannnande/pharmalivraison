import React,{useState} from 'react';
import {useNavigate} from 'react-router-dom';
import './Dashboard.css';
function Dashboard(){const navigate=useNavigate();const stats={waiting:5,today:23,revenue:145000,avgOrder:6300};const orders=[{id:'CMD001',client:'Jean Kouassi',items:3,total:12500,time:'5 min'},{id:'CMD002',client:'Marie Diabate',items:2,total:8500,time:'12 min'},{id:'CMD003',client:'Koffi Yao',items:5,total:18500,time:'18 min'}];return(<div className="dashboard-pharma"><div className="header-pharma"><div className="header-left"><h1>⚕️ Dashboard</h1><p>Pharmacie Plateau</p></div><div className="header-right"><button className="btn-stock" onClick={()=>navigate('/stock')}>📦 Stock</button><button className="btn-stats" onClick={()=>navigate('/statistics')}>📊 Stats</button><button className="btn-profile" onClick={()=>navigate('/profile')}>👤</button></div></div><div className="stats-grid"><div className="stat-card red"><div className="stat-icon">⏱️</div><div className="stat-content"><h3>{stats.waiting}</h3><p>En attente</p></div></div><div className="stat-card blue"><div className="stat-icon">📦</div><div className="stat-content"><h3>{stats.today}</h3><p>Aujourd'hui</p></div></div><div className="stat-card green"><div className="stat-icon">💰</div><div className="stat-content"><h3>{stats.revenue.toLocaleString()} F</h3><p>Chiffre d'affaires</p></div></div></div><div className="orders-section"><h2>Commandes en attente</h2><div className="orders-list">{orders.map(order=>(<div key={order.id} className="order-card"><h3>{order.id}</h3><p>{order.client}</p><p>{order.items} articles - {order.total.toLocaleString()} F</p><div className="order-actions"><button className="btn-accept">✅ Accepter</button><button className="btn-reject">❌ Refuser</button></div></div>))}</div></div></div>);}
export default Dashboard;